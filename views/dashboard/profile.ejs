<% layout("layouts/boilerplate") %>

<div class="container my-5 profile-wrapper">
  <div class="row g-4">
    
    <div class="col-lg-4">
      <div class="card border-0 shadow-sm p-4 rounded-4 profile-sidebar">
        <div class="text-center mb-4">
          <div class="profile-avatar-big mx-auto mb-3">
            <%= currentUser.username.charAt(0).toUpperCase() %>
          </div>
          <h3 class="fw-bold mb-0"><%= currentUser.username %></h3>
          <p class="text-muted small"><%= currentUser.role === 'admin' ? '🛡️ Administrator' : 'Verified Host' %></p>
        </div>

        <div class="profile-stats d-flex justify-content-around py-3 border-top border-bottom mb-4">
          <div class="text-center">
            <span class="fw-bold d-block h5 mb-0">0</span>
            <small class="text-muted extra-small">Bookings</small>
          </div>
          <div class="text-center">
            <span class="fw-bold d-block h5 mb-0">0</span>
            <small class="text-muted extra-small">Reviews</small>
          </div>
        </div>

        <div class="profile-info">
          <div class="d-flex align-items-center mb-3">
            <span class="material-symbols-outlined me-3 text-secondary">mail</span>
            <span class="small text-dark">Contact support to change email</span>
          </div>
          <div class="d-flex align-items-center mb-3">
            <span class="material-symbols-outlined me-3 text-secondary">verified_user</span>
            <span class="small text-dark">Identity Verified</span>
          </div>
        </div>

        <button class="btn btn-black w-100 mt-3 py-2 fw-bold">Edit Profile</button>
      </div>
    </div>

    <div class="col-lg-8">
      <div class="card border-0 shadow-sm p-4 rounded-4 mb-4">
        <h4 class="fw-bold mb-4">Welcome back, <%= currentUser.username %>!</h4>
        <p class="text-muted">Manage your airHost account, check your listing status, and update your preferences here.</p>
        
        <div class="row g-3 mt-2">
          <div class="col-md-6">
            <div class="activity-card p-3 rounded-3 border">
              <span class="material-symbols-outlined fs-2 mb-2 text-dark">home_work</span>
              <h6 class="fw-bold mb-1">Your Listings</h6>
              <p class="extra-small text-muted mb-2">View and manage properties you've added.</p>
              <a href="/user/listings" class="small fw-bold text-dark text-decoration-none hover-link">View all →</a>
            </div>
          </div>
<div class="col-md-6">
  <div class="activity-card p-3 rounded-3 border bg-white shadow-sm h-100">
    <span class="material-symbols-outlined fs-2 mb-2 text-primary">receipt_long</span>
    
    <h6 class="fw-bold mb-1 text-dark">Reservation Ledger</h6>
    
    <p class="extra-small text-muted mb-3">
      Track guest payouts, verified identity logs, and live booking settlements.
    </p>

    <a href="/dashboard/reservations" class="small fw-bold text-dark text-decoration-none hover-link d-flex align-items-center">
      View Transaction History <span class="material-symbols-outlined fs-6 ms-1">arrow_right_alt</span>
    </a>
  </div>
</div>

          <div class="col-md-6">
            <div class="activity-card p-3 rounded-3 border">
              <span class="material-symbols-outlined fs-2 mb-2 text-dark">settings</span>
              <h6 class="fw-bold mb-1">Account Settings</h6>
              <p class="extra-small text-muted mb-2">Update security and privacy preferences.</p>
              <a href="/help" class="small fw-bold text-dark text-decoration-none hover-link">Configure →</a>
            </div>
          </div>
        </div>
      </div>

      <div class="card border-0 shadow-sm p-4 rounded-4 bg-dark text-white">
        <div class="d-flex justify-content-between align-items-start">
          <div>
            <h5 class="fw-bold">Hosting Tip</h5>
            <p class="small text-white-50 mb-0">High-quality photos increase booking rates by 40%. Update your property images today!</p>
          </div>
          <span class="material-symbols-outlined fs-1 text-white-50">lightbulb</span>
        </div>
      </div>
    </div>

  </div>
</div>

<style>
  .profile-wrapper {
    min-height: 80vh;
  }

  /* Profile Card Styles */
  .profile-sidebar {
    position: sticky;
    top: 100px;
  }

  .profile-avatar-big {
    width: 100px;
    height: 100px;
    background: #000;
    color: #fff;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 3rem;
    font-weight: 800;
    box-shadow: 0 10px 20px rgba(0,0,0,0.15);
  }

  .extra-small {
    font-size: 0.75rem;
    text-transform: uppercase;
    letter-spacing: 0.5px;
  }

  /* Activity Cards */
  .activity-card {
    transition: all 0.3s ease;
    background: #fff;
    cursor: pointer;
  }

  .activity-card:hover {
    border-color: #000 !important;
    box-shadow: 0 5px 15px rgba(0,0,0,0.05);
    transform: translateY(-3px);
  }

  /* Universal Black Button */
  .btn-black {
    background: #000;
    color: #fff;
    border-radius: 10px;
    transition: 0.3s;
  }

  .btn-black:hover {
    background: #222;
    color: #fff;
    transform: scale(1.02);
  }

  .hover-link:hover {
    color: #ff385c !important;
  }
</style>